services:
  lifeledger:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - lifeledger-data:/app/data
    environment:
      SeedDemoData: "true"
    restart: unless-stopped

volumes:
  lifeledger-data:
